import React from "react"

function Header(){

    return (
        <header style={styles.header}>
            are u here
        </header>
    )
}

const styles = {
    header: {
        top: 0,
        width: "100%"
    }
}

export default Header